
package Pantallas;
import java.util.*;
import Modelo.FormulaGeneral;
public class PruebaFormulaGeneral {

    public static void main(String[] args) {               
                                
        //Declarar un ArrayList de tipo "FormulaGeneral" 
       /* 
        ArrayList <FormulaGeneral> listaFormula = new ArrayList<FormulaGeneral>();
       
       //Agregar datos al arrayList instanciado la clase las veces necesarias
       listaFormula.add(new FormulaGeneral(6,5,6,2,2,3));
      

       
       //=============USANDO FOREACH
       for (FormulaGeneral f: listaFormula){
           System.out.println(f.ImprimirFormula());
       }
       
       System.out.println("=======================");
              
       //--USANDO FOR NORMAL=======================
       for(int i =0; i<=listaFormula.size();i++){
           FormulaGeneral f = listaFormula.get(i);
           System.out.println(f.ImprimirFormula());
       }
       
       System.out.println("=======================");
       
       //========================USANDO ITERATOR================
       Iterator <FormulaGeneral> mi_iterador = listaFormula.iterator();
       while(mi_iterador.hasNext()){
           System.out.println(mi_iterador.next().ImprimirFormula());
       }
       
       //========================CON ARRAY NORMAL==========
       System.out.println("=======================");
       
       FormulaGeneral listaFormulaNormal[] = new FormulaGeneral[listaFormula.size()];
       listaFormula.toArray(listaFormulaNormal);       
       for(int i=0;i<listaFormulaNormal.length;i++){
        System.out.println(listaFormulaNormal[i].ImprimirFormula());
    }
    */
       
       
       
       
       
       
    }
    
}
